{
  "lock": false,
  "fmt": {
    "lineWidth": 120
  },
  "tasks": {
    "check": "deno cache --check=all *.ts",
    "dev": "deno fmt && deno lint && deno task check",
    "test": "deno test --no-check"
  }
}
